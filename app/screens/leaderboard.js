import { Text, View } from 'react-native'

export const Leaderboard = () => {
  // HINT: Server here:
  // fetch('http://localhost:4000/api/scores')

  return (
    <View style={{ flex: 1, padding: 12 }}>
      <Text>Your solution goes here...</Text>
    </View>
  )
}
